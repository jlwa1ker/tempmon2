// This file is generated during the build process.
// Major and minor come from version.properties; build comes from CI.
export const VERSION = {
  major: 1,
  minor: 0,
  build: 0,
  toString() {
    return `${this.major}.${this.minor}.${this.build}`
  }
}
